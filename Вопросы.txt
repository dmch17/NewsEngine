Lesson 02:

1) Не могли бы вы в начале следующего урока изложить принципы маппинга, моторый по умолчанию делает приложение asp.net mvc, если они существуют? Просто мне сейчас приходится запоминать, как сделать так, чтобы компилятор корректно смаппил, например, поля модели и элементы управления cshtml.

Вот, например, маппинг контроллера и представления. В ActionLink указывается не контроллер, а некая подстрока из его названия (не HomeController, а Home), а сам компилятор, насколько я понимаю, отбрасывает Controller и таким экзотическим способом маппит.

Или другой пример: в представлении заполняю выпадающий список.

<p>Операция: @Html.DropDownList("Operation", (List<SelectListItem>)ViewBag.Operations)</p>

Вот каким образом происходит передача параметра enum Operation модели, если список содержит только текстовые данные. Он тупо по тексту маппит? И таких вот неявных маппингов куча. Их все придется запоминать? Типа вот в этой ситуации, если я наберу назвение параметра, он смаппится с полем класса модели, а вот в другой - не смаппится.

2) Маппится по названию только свойства полей модели? Или еще и поля?
3) Правильно ли я понимаю, что в конструкции:

@using(Html.BeginForm("CalcForm","Calculator", FormMethod.Post))
{
	//
}

FormMethod служит для маппинга с аннотациями методов контроллера (типа [HttpGet])?
То есть если я на форме нажму кнопку, то к серверу отправится запрос по http post, и затем контроллер вызовет метод с аннотацией [HttpPost]?
4) Можно ли в след раз как домашку высылать только ссылку на git репозиторий? Или лучше как сейчас высылать весь архив?